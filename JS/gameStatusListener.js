import {myUID} from "./auth.mjs";

function gameStatusListener(gameOwner) {
    // if (gameOwner === myUID) $(ASSETS_WRAPPER).append($('<button id="startGame">Начать игру!</button>'));
}

export { gameStatusListener }